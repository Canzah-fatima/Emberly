import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { motion } from 'framer-motion';
import { Eye, EyeOff, ArrowRight } from 'lucide-react';
import { useAuth } from '../context/AuthContext';
export default function Login() {
  const { login } = useAuth(); const navigate = useNavigate(); const [identifier,setIdentifier]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [submitting,setSubmitting]=useState(false); const [show,setShow]=useState(false);
  const submit=async(e)=>{e.preventDefault();setSubmitting(true);setError('');try{await login(identifier,password);navigate('/')}catch(err){setError(err.response?.data?.error||'Could not log in. Please try again.')}finally{setSubmitting(false)}};
  return <main className="min-h-screen bg-emberly-ivory text-emberly-ink lg:grid lg:grid-cols-[1.05fr_.95fr]"><section className="relative hidden overflow-hidden bg-emberly-navy lg:flex lg:flex-col lg:justify-between p-12 xl:p-16"><div className="absolute -right-28 -top-28 h-72 w-72 rounded-full bg-emberly-crimson"/><div className="absolute bottom-16 -left-24 h-64 w-64 rounded-full border-[38px] border-emberly-blue/70"/><Link to="/" className="relative font-display text-3xl font-semibold text-emberly-ivory">Emberly<span className="text-emberly-crimson">.</span></Link><div className="relative max-w-xl"><p className="mb-5 text-xs font-bold uppercase tracking-[.28em] text-emberly-blue">Moments, made yours</p><h1 className="font-display text-6xl leading-[.94] text-emberly-ivory xl:text-7xl">Come back to what <span className="text-emberly-crimson-soft">inspires</span> you.</h1><p className="mt-7 max-w-md text-sm leading-6 text-emberly-ivory/55">Share moments, discover people, and keep your world close.</p></div><p className="relative text-xs text-emberly-ivory/35">© {new Date().getFullYear()} Emberly</p></section><section className="flex min-h-screen items-center justify-center px-5 py-10 sm:px-10"><motion.div initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="w-full max-w-md"><Link to="/" className="mb-12 block font-display text-3xl font-semibold text-emberly-navy lg:hidden">Emberly<span className="text-emberly-crimson">.</span></Link><p className="text-[10px] font-bold uppercase tracking-[.25em] text-emberly-crimson">Welcome back</p><h2 className="mt-2 font-display text-4xl font-semibold tracking-tight">Sign in to Emberly.</h2><p className="mt-2 text-sm text-emberly-ink/55">Your people and moments are waiting.</p><form onSubmit={submit} className="mt-8 space-y-4"><label className="block"><span className="mb-2 block text-xs font-semibold">Email or username</span><input required value={identifier} onChange={e=>setIdentifier(e.target.value)} className="w-full rounded-xl border border-emberly-navy/15 bg-white px-4 py-3 text-sm outline-none transition focus:border-emberly-navy" /></label><label className="block"><span className="mb-2 block text-xs font-semibold">Password</span><div className="relative"><input required type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-xl border border-emberly-navy/15 bg-white px-4 py-3 pr-12 text-sm outline-none transition focus:border-emberly-navy"/><button type="button" onClick={()=>setShow(v=>!v)} className="absolute right-2 top-1/2 -translate-y-1/2 grid h-9 w-9 place-items-center rounded-lg text-emberly-navy/45 hover:bg-emberly-ivory" aria-label={show?'Hide password':'Show password'}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<p role="alert" className="rounded-xl bg-emberly-crimson/10 px-3 py-2 text-sm text-emberly-crimson-dark">{error}</p>}<button disabled={submitting} className="group flex w-full items-center justify-center gap-2 rounded-xl bg-emberly-navy px-4 py-3.5 text-sm font-semibold text-emberly-ivory transition hover:bg-emberly-navy-deep disabled:opacity-50">{submitting?'Signing in…':'Sign in'}<ArrowRight size={17} className="transition group-hover:translate-x-1"/></button></form><p className="mt-7 text-center text-sm text-emberly-ink/55">New here? <Link to="/register" className="font-semibold text-emberly-crimson hover:underline">Create an account</Link></p></motion.div></section></main>;
}
