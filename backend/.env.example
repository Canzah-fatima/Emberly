NODE_ENV=development
PORT=5050
JWT_SECRET=replace-with-a-long-random-secret
FRONTEND_ORIGIN=http://localhost:5173

# Required for media uploads (posts, stories, avatars).
# Without these, the app runs but image/video upload endpoints return 503.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
